name: ReNamery Full Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    name: Build and Release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-22.04]
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.38.7'

      # --- Windows ビルド ---
      - name: Build Windows
        if: matrix.os == 'windows-latest'
        run: |
          flutter build windows --release
          Compress-Archive -Path build\windows\x64\runner\Release\* -DestinationPath ReNamery_Windows.zip

      # --- Android ビルド ---
      - name: Build Android
        if: matrix.os == 'windows-latest'
        run: |
          flutter build apk --release
          mv build/app/outputs/flutter-apk/app-release.apk ReNamery_Android.apk

      # --- Linux ビルド ---
      - name: Build Linux and Debian Package
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libblkid-dev liblzma-dev libgcrypt20-dev pkg-config
          chmod +x scripts/package_debian.sh
          ./scripts/package_debian.sh
          # ポータブル版の作成（スクリプト内のビルド成果物を使用）
          cd build/linux/x64/release/bundle
          tar -czf ../../../../../ReNamery_Linux.tar.gz .

      # --- GitHub へのアップロード ---
      - name: Upload Release Assets
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ReNamery_Windows.zip
            ReNamery_Android.apk
            ReNamery_Linux.tar.gz
            build/debian/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
