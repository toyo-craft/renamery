# ReNamery ユーザーマニュアル

> [!WARNING]
> **ベータ版およびWeb版に関する重要事項**
> - 本アプリは現在ベータ版です。ファイル名の禁止文字チェックや重複チェックが未実装のため、誤った操作によりファイルが破損したり、予期せぬエラーが発生する可能性があります。
> - **Webデモ版**では、ブラウザ上の疑似環境での動作となり、実際のファイルは変更されません。また、履歴保存や「元に戻す」機能も利用できません。
> - 大切なファイルを扱う際は、必ず事前にバックアップを取ってください。

ReNamery（リネームリー）は、名作リネームソフト「Namery」をリスペクトし、モダンなインターフェースで復刻・再構築したファイル名変更ツールです。
Windows / Android / Chromebook / Webデモ版 で動作します。

## UI構成

アプリは大きく3つのエリアで構成されています。
- **左パネル**: フォルダツリー（クイックアクセス）と、選択したファイルの内容を確認できる**プレビュー画面**があります。
- **中央パネル**: ファイルリスト。現在のファイル名と変更後のプレビュー名が並んで表示されます。
- **右パネル（サイドパネル）**: リネームのルールを設定する各種パネルが並んでいます。

## リネーム設定（右パネル）

右パネルにある各機能パネルを使用して、ファイル名の変更ルールを作成します。

### 1. 文字列・連番
- **文字列**: ファイル名に特定の文字を追加します。
- **連番**: `001` から始まる番号などを付与します。
  - **文字列 + 連番**: 指定した文字の後に番号を付けます。
  - **現在名 + 連番**: 元の名前の後に番号を付けます。
  - **連番 + 文字列 / 連番 + 現在名**: 名前の先頭に番号を付けます。

### 2. 置換・挿入・削除
- **置換**: 特定の文字を別の文字に置き換えます。
- **正規表現置換**: 正規表現による高度な置換が可能です。
  - ※注意: `\1` などのバックリファレンス（後方参照）は現在サポートされていません。
- **大文字・小文字変換**: 置換パネル内のオプションから、一括で大文字化/小文字化が行えます。
- **挿入**: 指定した位置に文字を割り込ませます。
- **削除**: 「先頭からN文字」「指定文字まで」などの条件で文字を削ります。

### 3. 拡張子 (Extension)
- **拡張子の変更**: `txt` を `md` に変えるなどの操作。
- **追加・削除**: 拡張子を付け足したり、完全に取り除いたりします。
- **大文字・小文字統一**: 拡張子部分のみを大文字/小文字に揃えます。

### 4. 日付・変換 (Extra)
- **日付付与**: ファイルの作成日や更新日を名前の先頭や末尾に付加します。
- **全角/半角変換**: 英数字やカタカナの全角・半角を変換します。
  - ※制限: 一部環境で半角カタカナが豆腐文字（□）になる不具合があります。
- **ひらがな/カタカナ変換**: 日本語の文字種を変換します。

### 5. 属性・タイムスタンプ (Etc)
**※この操作は「元に戻す」ことができません。**
- **属性変更**: 読み取り専用や隠しファイル設定を切り替えます（Windowsのみ）。
- **タイムスタンプ変更**: ファイルの更新日時を任意の日時に書き換えます。

---

## 制限事項・既知の不具合（重要）

### 安全性・バリデーション
- **チェック機能の欠如**: 現在、ファイル名に使えない文字（`\ / : * ? " < > |`）の入力チェックや、同名ファイルによる重複エラーの防止機能がありません。実行前にプレビューをよく確認してください。

### 未実装・動作不安定な機能
- **ピン留めアイコン**: 連番をリセットせずに維持する機能は現在利用できません。
- **リストリネーム**: テキストリストを貼り付けてリネームする機能は現在未実装です。
- **フォルダのリネーム**: 現在はファイルのみが対象です。フォルダ自体の名前変更はできません。
- **フォルダ関連の操作**: 中央リスト内でのダブルクリックによる階層移動、およびフォルダを優先したソート順（現在はファイルと混ざります）は未実装です。

### 操作
- **Undo（元に戻す）**: リネーム実行後、`Ctrl+Z` で取り消せます。
- **ただし、属性・日付変更、およびWeb版での操作は元に戻せません。**

---

## ライセンス
本ソフトウェアは **MIT License** のもとで公開されています。

**Copyright (c) 2026 Toyo Craft Lab**

詳細は `LICENSE` ファイルをご覧ください。

## 開発者情報
**企画・開発**: Toyo Craft Lab  
**URL**: https://toyo-craft.net/  
当アプリは、名作フリーソフト「Namery」に最大限のリスペクトを捧げ作成されました。
