{
  "name": "renamery-e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:web": "flutter build web",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:full": "flutter build web && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "http-server": "^14.1.1"
  }
}
